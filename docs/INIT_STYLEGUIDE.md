# 📦 `__init__`-стайлгайд

Єдині правила оформлення файлових ініціалізаторів пакетів (`__init__.py`) у `app/*`. Орієнтир — `src/app/errors/__init__.py`.

---

## 🧱 Обов’язковий каркас
1. **Коментар з емодзі й шляхом** (`# 📄 errors/__init__.py`).
2. **Докстринг українською** з коротким описом пакета + маркований список його ролей.
3. **Імпорти** згруповані логічно (зовнішні → системні → внутрішні). Для більшості `__init__.py` достатньо «внутрішніх» імпортів, але блок все одно позначаємо коментарем.
4. **Пояснювальні коментарі** до кожного імпорту (коротко про роль класу/функції).
5. **Секція `__all__`** з «парканом»:
   ```
   # ================================
   # 📤 ПУБЛІЧНИЙ API ПАКЕТА
   # ================================
   __all__ = [...]
   ```
   - Компоненти згруповані тематично (наприклад, «Ошибки» / «Сервіс»).
   - Коментарі перед групами допомагають читачу.

---

## 🧭 Призначення `__init__.py`
- Надати **єдину точку імпорту** для зовнішніх модулів.  
- Явно задокументувати **публічний API пакета** через `__all__`.  
- Приховати внутрішню структуру каталогу (споживачі імпортують лише з пакета, а не з файлів).  
- Передати коротку інформацію про пакет через докстринг (роль, ключові компоненти, контекст).

---

## 🧩 Структура імпортів
- Якщо пакет імпортує тільки внутрішні файли, використовуємо блок:
  ```python
  # 🧩 Внутрішні модулі проєкту
  from .custom import CustomClass
  ```
- Кожен імпорт має коментар із емодзі (`# 🛠️ ...`), щоб миттєво зрозуміти його роль.
- Не виконуємо імпортів із побічними ефектами (жодного виконання коду при імпорті пакета).

---

## 🧾 Докстринг і коментарі
- Докстринг лаконічний (2–3 речення) + маркований список ролей (`🔹` або `•`).  
- Якщо пакет публікує кілька груп компонентів (наприклад, «помилки» та «обробник»), це видно в докстринзі й `__all__`.  
- Інлайн-коментарі українською; вирівнюємо табами, як у загальному стайлгайді.

---

## ✅ Чекліст перед комітом
- [ ] Є шапка з емодзі та шляхом.
- [ ] Докстринг описує пакет і ролі компонентів.
- [ ] Імпорти згруповані, мають коментарі.
- [ ] `__all__` містить лише публічні сутності; групи підписані.
- [ ] Немає побічних ефектів — лише імпорти та декларації.
- [ ] Візуально файл нагадує `src/app/errors/__init__.py`.

---

Дотримуйтеся цього гіда, щоб усі пакети експортували свої API однаково та залишалися читабельними для всієї команди. 😊
