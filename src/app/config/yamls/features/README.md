# 🎯 app/config/yamls/features
"""
🎯 Конфіги конкретних фіч, які перекривають `base/` (але самі можуть бути override-нуті `local/`).

🔹 Виносимо сюди параметри, що стосуються окремих підсистем (музика, image generation, Telegram)
🔹 Підключаються **після** `base/`, тому мають вищий пріоритет
🔹 Допомагають ізолювати великі блоки конфігів без зміни глобальної бази
"""

---

## 📂 Склад каталогу

| Файл | Призначення |
| --- | --- |
| `50_music.yaml` | Поведінка музичної підсистеми: AI‑рекомендації, кеш, скачування YouTube/файловий менеджер (`MusicRecommendation`, `MusicSender`). |
| `90_image_generation.yaml` | Налаштування генератора таблиць/шрифтів/фонту (FontService, SizeChart таблиці, імпорт шрифтів). |
| `95_telegram.yaml` | HTTP/long-poll тайм-аути, ретраї та throttling для Telegram Bot API (`telegram.http.*`). |

---

## 🧭 Поради
- При додаванні нової фічі з помітним набором параметрів створіть окремий файл з індексом у цьому каталозі (`55_new_feature.yaml`).
- Якщо потрібно локально змінити значення на оточенні — додавайте YAML у `local/` з тим самим ключем (має вищий пріоритет).
