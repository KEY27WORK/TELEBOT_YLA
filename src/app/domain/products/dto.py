# 📦 app/domain/products/dto.py
"""
📦 DTO для домену товарів (легкі структури без повного парсингу).

🔹 `ProductHeaderDTO` — короткий заголовок із URL та превʼю-зображенням.
"""

from __future__ import annotations

# 🔠 Системні імпорти
import logging                                                  # 🧾 Логування факту ініціалізації DTO
from dataclasses import dataclass                               # 🧱 Використовуємо dataclass для простих контейнерів
from typing import Optional                                     # 🧰 Анотації optional-полів

# 🧩 Внутрішні модулі
from .entities import Url                                       # 🔗 Строгий тип URL домену


# ================================
# 🪵 ЛОГЕР МОДУЛЯ
# ================================
logger = logging.getLogger(__name__)                            # 🧾 Локальний логер для domain-level DTO


# ================================
# 🧱 DTO
# ================================
@dataclass(frozen=True, slots=True)
class ProductHeaderDTO:
    """
    Легкий заголовок товару (назва, зображення, посилання) без повного парсингу.
    """

    title: str                                                   # 🏷️ Назва товару
    image_url: Optional[str]                                     # 🖼️ Превʼю-зображення (може бути None)
    product_url: Url                                             # 🔗 Канонічне посилання на товар


logger.debug("🧾 ProductHeaderDTO завантажено")                  # 🧾 Фіксуємо факт імпорту DTO
