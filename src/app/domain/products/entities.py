# 📦 entities.py
"""
📦 entities.py — Доменные сущности для продуктов.

Определяет чистую структуру данных для товара, используемую во всей бизнес-логике.
Этот объект не зависит от парсеров или API.
"""

# 🔠 Системные импорты
from dataclasses import dataclass, field
from typing import Dict, List

# ==========================
# 🏛️ СУЩНОСТЬ ПРОДУКТА
# ==========================
@dataclass
class ProductInfo:
    """
    Содержит всю информацию о товаре.

    Является "тупым" объектом данных и не содержит логики взаимодействия
    с внешним миром (сеть, файлы и т.д.).
    """
    # --- Обязательные поля ---
    title: str
    price: float

    # --- Необязательные поля с sane defaults ---
    description: str = "Опис відсутній"
    image_url: str = ""
    weight: float = 0.5  # Средний вес по умолчанию
    colors_text: str = "" # Старое поле, может быть удалено в будущем
    images: List[str] = field(default_factory=list)
    currency: str = "USD"
    sections: Dict[str, str] = field(default_factory=dict)

    # --- Данные о наличии ---
    stock_data: Dict[str, Dict[str, bool]] = field(default_factory=dict)