# 🎯 facades — допоміжні обгортки

Пакет містить тонкі фасади, що інкапсулюють роботу з окремими підсистемами (availability, music) та повертають прості DTO для оркестраторів.

---

## 📂 Структура

```bash
📦 facades/
├── 📘 README.md                 # цей файл: опис фасадів
├── 📄 __init__.py               # експортує публічні фасади/DTO
├── 📄 availability_facade.py    # обгортка над AvailabilityProcessingService
└── 📄 music_facade.py           # обгортка над MusicRecommendation
```

---

## 🧩 Фасади

- `AvailabilityFacade`  
  • викликає `AvailabilityProcessingService.process`;  
  • форматує заголовок та текст кольорів/розмірів;  
  • повертає `AvailabilityResult (header, colors_text)` або порожні значення.

- `MusicFacade`  
  • формує `ProductPromptDTO` з `ProductInfo`;  
  • бере перший релевантний трек із `MusicRecommendation`;  
  • повертає `MusicSuggest (title, url)` або `None`, якщо рекомендацій немає.

---

## 📌 Призначення

Фасади дозволяють оркестраторам (`ProductProcessingService`) працювати з простими структурами, ізолюючи всі нюанси DTO, логування та обробки помилок усередині кожного фасаду. Це спрощує тестування та заміну підсистем.
