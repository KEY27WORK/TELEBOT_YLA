# 🧭 STYLEGUIDE.md — Єдиний стиль для проєкту
"""
🧭 Цей гайд описує обов’язкові правила оформлення Python-файлів у проєкті.

🔹 Основою є файл `src/app/bot/commands/core_commands_feature.py`.
🔹 Будь-які нові або відрефакторені модулі мають відтворювати цей стиль.
🔹 Документ призначений як чеклист для асистента, який переписує код.
"""

---

## 1️⃣ Заголовок + докстринг (обов’язково)
- Перша строка файлу — коментар з емодзі та абсолютним шляхом (`# 📬 app/bot/...`).
- Після коментаря — докстринг українською з коротким описом призначення модуля.
- Докстринг містить маркований список (кожен пункт з емодзі) з ключовими ролями модуля.

```
# 📬 app/bot/commands/core_commands_feature.py
"""
📬 Опис файлу однією фразою.

🔹 Пункт 1
🔹 Пункт 2
"""
```

---

## 2️⃣ Структура імпортів
- Імпорти організовуються у три блоки з коментарями-заголовками:
  1. `# 🌐 Зовнішні бібліотеки`
  2. `# 🔠 Системні імпорти`
  3. `# 🧩 Внутрішні модулі проєкту`
- Порожній рядок між блоками; в кожному блоці імпорти впорядковані за абеткою.
- Кожен імпорт завершується вирівняним коментарем із поясненням (за потреби).

---

## 3️⃣ Загальний каркас класів/функцій
- Перед великими секціями використовуємо «паркан» із трьох рядків:
```
# ================================
# 🏛️ НАЗВА СЕКЦІЇ
# ================================
```
- Основні розділи:
  - ФІЧА / ГОЛОВНИЙ КЛАС
  - ІНІЦІАЛІЗАЦІЯ
  - РЕЄСТРАЦІЯ ОБРОБНИКІВ / API
  - КОМАНДИ / ОСНОВНІ МЕТОДИ
  - CALLBACK-и / ДОПОМІЖНІ МЕТОДИ

---

## 4️⃣ Docstring-и та коментарі
- Усі docstring-и пишемо українською, у Google-style, коротко пояснюючи призначення.
- Опис параметрів (`Args:`) та повертаних значень (`Returns:`) додаємо лише за потреби.
- Інлайн-коментарі та docstring-и завжди українською. Коментарі вирівнюємо табами до однієї вертикалі (стовпчика) і супроводжуємо релевантним емодзі (у дусі `# ⚙️ Зберігаємо константи застосунку`).
- **Обов’язково** ставимо інлайн-коментар з емодзі до кожного:
  - присвоєння, навіть якщо змінна очевидна (`counter = 0										# 🔢 Лічильник помилок`);
  - виклику функції/методу (якщо їх кілька — пояснюємо кожен);
  - умовного оператора, циклу, `return`, `continue`, `break`;
  - побудови структур даних (словники/списки/comprehension тощо).
- Навіть для «порожніх» ініціалізацій додаємо короткий коментар (`result: Dict[...] = {}							# 📦 Порожній результат для заповнення`).
- **Обов’язково** ставимо інлайн-коментар з емодзі до кожного:
  - присвоєння, навіть якщо назва змінної очевидна (`counter = 0                                     # 🔢 Лічильник помилок`);
  - виклику функції/методу, окрім найпростіших (але краще зазначити контекст);
  - умовного оператора, циклу, `return`, `continue`, `break`;
  - елемента структур даних (словники, comprehension, побудова списків).
- Навіть для «порожніх» ініціалізацій додаємо короткий коментар (`result: Dict[...] = {}               # 📦 Фінальний словник результатів`).
- Докстринг класу — лаконічне резюме (1–2 речення); докстринг методів пояснює дію, додатково можна описати ключові кроки.

---

## 5️⃣ Типізація
- Усі аргументи і повернені значення мають анотації.
- Використовуємо конкретні типи (`Dict[str, CallbackHandlerType]`), не загальні `Dict`.
- Для необов’язкових параметрів явно застосовуємо `Optional[...]`.

---

## 6️⃣ Логування
- Ініціалізація логера: `logger = logging.getLogger(LOG_NAME)`.
- Логи українською; в info — кроки сценарію, у warning/error — проблеми з контекстом.
- Формат повідомлень використовує емодзі для швидкої ідентифікації (наприклад, `logger.info("ℹ️  ...")`).

---

## 7️⃣ Стиль коду
- Вирівнювання присвоєнь та параметрів — через таби, щоб коментарі були в одній колонці.
- Блоки умов/циклів містять короткі коментарі перед важливими діями.
- Константи/імена атрибутів зрозумілі та англомовні, текст у логах — український.
- Порожні рядки розставляються для читабельності (між методами, секціями).

---

## 8️⃣ Алгоритм для рефакторингу файлів
1. Додай заголовок з емодзі та шляхом.
2. Напиши докстринг, що віддзеркалює роль модуля у форматі, як вище.
3. Розбери імпорти на три групи з відповідними коментарями.
4. Побудуй скелет класу/функцій із секційними «парканами».
5. Перевір, що всі методи мають українські docstring-и.
6. Додай/вирівняй інлайн-коментарі з емодзі.
7. Упевнись у правильній типізації та використанні українських логів.
8. Перечитай файл, щоб стиль збігався з еталоном `core_commands_feature.py`.

---

## 9️⃣ Перевірка перед здачею
- Заголовок та докстринг присутні.
- Імпорти згруповані та задокументовані.
- Секційні розділювачі оформлені.
- Docstring-и та інлайн-коментарі українською, з емодзі.
- Типи додають ясності; відсутні `Any`, якщо можна конкретизувати.
- Логи зрозумілі, містять контекст.
- Код візуально нагадує еталон `core_commands_feature.py`.

---

> Виконуй кожну зміну згідно з цим гайдом, щоби вся кодова база залишалася в єдиному читабельному стилі. Еталоном є `src/app/bot/commands/core_commands_feature.py`. Будь-які відхилення потрібно узгодити окремо. 😊
