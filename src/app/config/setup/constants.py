# 📖 constants.py — Централізований файл констант.
"""
📖 constants.py — Централізований файл констант для проєкту.

🔹 Зберігає текстові мітки для кнопок меню.
🔹 Визначає рядкові ідентифікатори для режимів роботи бота.
🔹 Допомагає уникнути "магічних рядків" у коді.
"""

# 🔠 🧱 Системні імпорти
import re 

# ================================
# 🔵 КНОПКИ ГОЛОВНОГО МЕНЮ
# ================================
# Текстові мітки, що відображаються на кнопках у головному меню Telegram.
# Використовуються в `keyboards.py` для створення та в `menu_handler.py` для розпізнавання.

BTN_INSERT_LINKS = "🔗 Вставляти посилання товарів"
BTN_MY_ORDERS = "📦 Мої замовлення"
BTN_COLLECTION_MODE = "📚 Режим колекцій"
BTN_SIZE_CHART_MODE = "📏 Таблиця розмірів"
BTN_CURRENCY = "💱 Курс валют"
BTN_HELP = "❓ Допомога"
BTN_PRICE_CALC_MODE = "🧮 Режим розрахунку товару"
BTN_REGION_AVAILABILITY = "🌍 Перевірити розміри в регіонах"
BTN_DISABLE_MODE = "⏹️ Вимкнути режим"

# ================================
# ⚙️ ІДЕНТИФІКАТОРИ РЕЖИМІВ
# ================================
# Рядкові ключі, що зберігаються в `context.user_data["mode"]` для визначення
# поточного стану (режиму) роботи логіки в `LinkHandler`.

MODE_PRODUCT = "product"
MODE_COLLECTION = "collection"
MODE_SIZE_CHART = "size_chart"
MODE_REGION_AVAILABILITY = "region_availability"
MODE_PRICE_CALCULATION = "price_calculation"

# ================================
# ⚙️ ДОПОМІЖНА ФУНКЦІЯ
# ================================
def generate_menu_pattern() -> str:
    """
    Генерує regex-патерн з констант кнопок меню для їх розпізнавання.
    """
    # 📝 Створюємо список назв кнопок з констант
    buttons = [
        BTN_INSERT_LINKS, BTN_MY_ORDERS, BTN_COLLECTION_MODE,
        BTN_CURRENCY, BTN_SIZE_CHART_MODE, BTN_REGION_AVAILABILITY,
        BTN_PRICE_CALC_MODE, BTN_HELP, BTN_DISABLE_MODE
    ]
    # 🔍 Створюємо патерн, екрануючи спецсимволи та об'єднуючи через |
    return "^(" + "|".join(re.escape(btn) for btn in buttons) + ")$"
