# 🧩 Domain: Content
Пакет доменних контрактів, що описує генерацію хештегів для продуктів. Лише Protocol/DTO — жодних інфраструктурних залежностей.

---

## 📂 Структура
```bash
content/
├── 📘 README.md
├── __init__.py           # Реекспорт IHashtagGenerator для зручних імпортів
└── interfaces.py         # Протокол генератора хештегів, логування ініціалізації
```

---

## 🧱 Призначення
- Визначає асинхронний контракт `IHashtagGenerator` на основі `ProductInfo`.
- Забезпечує єдине джерело правди для типізації (повертає `Set[str]` без `#`).
- Дає змогу легко підміняти реалізації (AI, ручні словники, кеші) без зміни доменного коду.

---

## 🔌 Ключові компоненти
- **`IHashtagGenerator`** — `Protocol` з методом `generate(product: ProductInfo) -> Set[str]`.
- **Логер** — кожна реалізація може спиратися на префікс `domain.content.hashtags` для діагностики.

---

## 🚀 Приклад використання
```python
from app.domain.content import IHashtagGenerator
from app.domain.products.entities import ProductInfo

async def example(generator: IHashtagGenerator) -> None:
    product = ProductInfo(url="https://shop/item/42", title="YoungLA Tee")
    tags = await generator.generate(product)
    print("#" + " #".join(sorted(tags)))
```

---

## ✅ Примітки
- Контракт асинхронний: реалізація може звертатися до AI або кешу.
- Повернене `Set[str]` не містить символу `#` — формат додається вище у стеку.
- Реалізація не повинна виконувати I/O у `__init__`; усі побічні ефекти — всередині `generate`.
