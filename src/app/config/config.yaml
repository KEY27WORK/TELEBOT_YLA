# ⚙️ app/config/config.yaml
# Головний конфігураційний файл проєкту.
# Містить налаштування для всіх зовнішніх сервісів та інфраструктурних компонентів.

# ================================
# 🌍 РЕГІОНИ ТА ЇХ URL
# ================================
# Використовується в AvailabilityManager для мульти-регіональної перевірки.
regions:
  us:
    base_url: "https://www.youngla.com"
    currency: "USD"
  eu:
    base_url: "https://eu.youngla.com"
    currency: "EUR"
  uk:
    base_url: "https://uk.youngla.com"
    currency: "GBP"

# ================================
# 🛠️ ЗАГАЛЬНІ НАЛАШТУВАННЯ
# ================================
# Параметри, що раніше були в config.json
webdriver_path: "./chromedriver"                        # 📂 Шлях до драйвера браузера (якщо потрібен локально, для Playwright не використовується)
default_language: "uk"                                  # 🇺🇦 Мова за замовчуванням для перекладів та інтерфейсу
max_retries: 5                                          # 🔁 Максимальна кількість повторних спроб для невдалих мережевих запитів
collection_processing_delay_sec: 2                      # ⏱️ Затримка в секундах між обробкою товарів у колекції

collection_progress_update_interval: 5                  # 📊 Інтервал оновлення прогресу для користувача


# ================================
# 💱 НАЛАШТУВАННЯ API ВАЛЮТ
# ================================
# Використовується в CurrencyManager для отримання та кешування курсів.
currency_api:
  # URL для отримання курсів. Можна замінити на Monobank або інший сервіс.
  url: "https://api.monobank.ua/bank/currency"
  
  # Маржа, яка додається до курсу продажу (напр., 0.5 грн).
  margin: 0.5

  # Коди валют за ISO 4217, які використовуються в API Monobank.
  codes:
    USD: 840
    EUR: 978
    GBP: 826
    PLN: 985
  
  # Резервні курси на випадок, якщо API недоступний.
  fallback_rates:
    USD: 42.5
    EUR: 45.5
    GBP: 53.0
    PLN: 11.0

  # Налаштування для HTTP-запитів
  timeout_sec: 5
  retry_attempts: 2
  retry_delay_sec: 2

# ================================
# 🌍 НАЛАШТУВАННЯ ПЕРЕВІРКИ НАЯВНОСТІ
# ================================
availability:
  cache_ttl_sec: 300  # ⏳ Час життя кешу для звітів про наявність


# ================================
# 🌐 НАЛАШТУВАННЯ PLAYWRIGHT
# ================================
# Використовується в WebDriverService.
playwright:
  user_agent: "Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/122.0.0.0 Safari/537.36"
  headless: true                                            # true — без графічного інтерфейсу, false — для дебагу
  retry_attempts: 5                                         # Кількість повторних спроб при Cloudflare
  retry_delay_sec: 2                                        # Затримка між спробами в секундах
  
  # ✅ (ДОДАНО) Список фраз для виявлення блокування Cloudflare
  cloudflare_phrases:
    - "Your connection needs to be verified"
    - "Please complete the security check"
    - "Verifying you are human"

# ================================
# 🗂️ ШЛЯХИ ДО ЛОКАЛЬНИХ ФАЙЛІВ
# ================================
# Використовується різними сервісами для зберігання даних.
files:
  # Файл для зберігання локальної бази ваг товарів.
  weights: "weights.json"
  
  # Файл для кешування курсів валют.
  currency_rates: "current_rate.txt"

# ================================
# 🧾 НАЛАШТУВАННЯ ЛОГУВАННЯ
# ================================
logging:
  level: "DEBUG"        # Рівень логування: DEBUG, INFO, WARNING, ERROR
  console: true         # Вмикати/вимикати вивід у консоль (true/false)
  
  # Список бібліотек, логи яких потрібно "приглушити" до певного рівня
  suppress:
    httpx: "WARNING"
    telegram.ext: "WARNING"
    asyncio: "WARNING"

# ================================
# 🎨 НАЛАШТУВАННЯ ГЕНЕРАЦІЇ ЗОБРАЖЕНЬ
# ================================
image_generation:
  font_paths:
    bold:
      - "/System/Library/Fonts/Supplemental/Arial Bold.ttf"
      - "/System/Library/Fonts/Roboto-Bold.ttf"
      - "/System/Library/Fonts/Supplemental/Arial Bold.ttf"
      - "/usr/share/fonts/truetype/dejavu/DejaVuSans-Bold.ttf"
      - "/usr/share/fonts/truetype/liberation/LiberationSans-Bold.ttf"
      - "C:\\Windows\\Fonts\\arialbd.ttf"
      - "C:\\Windows\\Fonts\\Roboto-Bold.ttf"
    mono:
      - "/System/Library/Fonts/Supplemental/Arial.ttf"
      - "/usr/share/fonts/truetype/dejavu/DejaVuSansMono.ttf"
      - "/System/Library/Fonts/Supplemental/Courier New Bold.ttf"
      - "C:\\Windows\\Fonts\\courbd.ttf"