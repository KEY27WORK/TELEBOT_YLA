# 🧾 YAML_STYLEGUIDE.md — Єдиний стиль YAML-файлів
"""
🧾 Цей документ встановлює єдиний стиль для всіх конфігураційних YAML у `app/config/yamls/**`.

🔹 Шапка з шляхом, описом і контекстом модуля
🔹 Секційні «парканчики» для великих блоків
🔹 Українські коментарі з емодзі (коротко пояснюють значення)
🔹 Вирівняна структура ключів та акуратні списки
"""

---

## 1️⃣ Шапка кожного YAML
- Перша лінія: `# ⚙️ app/config/yamls/.../file.yaml` (повний шлях + емодзі).
- Друга/третя: короткий опис файлу та його ролі (`# 📸 OCR (OpenAI Vision)`).
- Далі 1–2 рядки з додатковим контекстом/важливими правилами.
- При потребі додавайте блок приміток (`# 🔗 Важливо: ...`).

Приклад:
```
# ⚙️ app/config/yamls/base/25_ocr.yaml
# 📸 OCR (OpenAI Vision)
# Налаштовує таймаути, ретраї та кешування для Vision API.
```

---

## 2️⃣ Секційні «парканчики»
Перед великими блоками додаємо трирядковий розділювач:
```
# ================================
# 🛠️ ЗАГАЛЬНІ НАЛАШТУВАННЯ
# ================================
```
- Емодзі має відображати зміст (🛠️ утиліти, 🔁 ретраї, 💾 кеш тощо).
- Парканчики використовуємо для кожної логічної секції (норми, тарифи, селектори, backoff).

---

## 3️⃣ Коментарі
- Використовуємо короткі україномовні коментарі з емодзі справа від значення:
  ```
  max_retries: 3     # 🔁 Скільки разів повторюємо запит
  ```
- Якщо значення складне, додаємо коментар перед блоком:
  ```
  tiers:                                                # 📊 Таблиця тарифів
    - max_kg: 0.5                                      # ⚖️ До 0.5 кг
  ```
- Не перевантажуйте поясненнями: максимум 1–2 фрази, головна думка.

---

## 4️⃣ Форматування
- Вирівнюємо масиви й словники згідно з YAML 2-х пробільними відступами.
- Значення, що містять спеціальні символи/пробіли, беремо в лапки.
- Числові значення залишаємо без лапок, якщо це чисті числа.
- Порожні рядки використовуємо для відділення секцій.

---

## 5️⃣ Порядок та назви
- Зберігаємо існуючий числовий префікс (00_, 10_, 15_, …).
- Всередині файлу групуємо ключі тематично (наприклад, `parser:`, `alt_text:`, `metrics:`).
- Для списків об'єктів додаємо `# ---` лише якщо потрібно візуально розділити великі блоки (опціонально).

---

## 6️⃣ Чекліст перед комітом
- [ ] Є шапка з шляхом та описом.
- [ ] Для кожної секції додано «паркан».
- [ ] Значущі значення мають короткі коментарі з емодзі.
- [ ] Вирівнювання та відступи акуратні.
- [ ] Не змінено реальне значення конфігу (лише формат/коментарі).
- [ ] Зміни переглянуті дифом (`git diff`), щоб переконатися у відсутності помилок.

---

Дотримуйтеся цього гайда, щоб усі YAML-файли були однаково читабельними для команди. Якщо потрібно додати новий пункт або приклад — оновіть цей документ. 😊
