# 🧮 General Size Charts
Керування універсальними таблицями розмірів YoungLA (чоловічі/жіночі) для пайплайна `size_chart_service`.

---

## 📂 Структура
```bash
general/
├── 📘 README.md
├── 📄 __init__.py
├── 📄 cache_service.py
├── 📄 gender_detector.py
└── 📄 types.py
```

---

## 🧭 Призначення
- Знаходити стать товару за вихідним HTML та артикулом (`gender_detector.py`).
- Визначати, яка універсальна таблиця (men/women) має бути використана для запиту.
- Кешувати вже локалізовані таблиці, щоби уникати повторних OCR/AI викликів.
- Надавати enums та протоколи для інших модулів `size_chart`.

---

## 🧩 Компоненти
- **`types.py`** — містить переліки `ProductGender` та `GeneralChartVariant`, які споживає `SizeChartService`.
- **`gender_detector.py`** — детектор статі товару на основі YoungLA SKU/handle з HTML.
- **`cache_service.py`** — фасад для кешування сгенерованих PNG таблиць (одна на variant).

---

## 🚀 Приклад використання
```python
from app.infrastructure.size_chart.general import (
    GeneralChartCache,
    ProductGender,
    YoungLAProductGenderDetector,
)

detector = YoungLAProductGenderDetector()
gender = detector.detect(page_source)

cache = GeneralChartCache("var/general_size_charts")
cached_path = cache.get_cached_path(variant)
```

---

## ✅ Примітки
- Кешування відбувається за ключем `men.png` та `women.png` у каталозі `SIZE_CHART_GENERAL_CACHE`.
- Детектор орієнтований на YoungLA: жіночі артикули починаються з `W`/`w`.
- За невідомої статі `SizeChartService` може використовувати обидва варіанти як fallback.
