# 🧠 Domain / AI / Interfaces
Доменно-орієнтований підпакет із контрактом побудови промтів і супутніми DTO/Enums. Саме ці типи імпортує решта домену, а реалізації живуть у `infrastructure/ai`.

---

## 📂 Структура
```bash
interfaces/
├── 📘 README.md
├── __init__.py              # Реекспорт контракту/DTO для зручних імпортів
└── prompt_service_interface.py  # Контракти + DTO/Enums для генерації промтів
```

---

## 🧭 Призначення
- Інкапсулює `IPromptService` — єдину точку для запитів на побудову промтів.
- Описує мультимодальні DTO (`ChatPrompt`, `ChatMessage`, `TextPart`, `ImagePart`).
- Уніфікує переліки `Tone`, `Lang`, `Role` для всіх LLM-фіч.
- Надає фабрики (`ChatPrompt.user_text/system_user_text`) для швидкого складання промтів.

---

## 🧱 Ключові файли
- **`__init__.py`** — реекспортує всі типи/контракти, щоб `from app.domain.ai import ...` працювало без зайвих шляхів.
- **`prompt_service_interface.py`** — містить реалізацію DTO, Enum-ів, фабрик та `IPromptService` з детальними докстрингами й логуванням.

---

## 🚀 Приклад використання
```python
from app.domain.ai.interfaces import IPromptService, ProductPromptDTO, Tone

async def build_slogan_prompt(builder: IPromptService) -> None:
    dto = ProductPromptDTO(
        title="YoungLA Oversized Tee",
        description="Чорна футболка оверсайз з бавовни",
        image_url="https://...",
    )
    prompt = builder.get_slogan_prompt(dto, tone=Tone.SALES)
    # prompt.messages вже містять system/user інструкції для LLM
```

---

## ✅ Примітки
- Файл не залежить від OpenAI/Gemini SDK — тільки типи та Protocol.
- Будь-які нові промт-фічі слід спершу описувати тут (DTO/Enum/метод у `IPromptService`).
- Реалізації сервісу повинні логуватися на рівні інфраструктури; домен лише формує контракти.
