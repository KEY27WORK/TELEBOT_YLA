# 📁 app/assets/fonts
Каталог для шрифтів, які обов’язково мають бути поруч із кодом і доступними у будь-якому контейнері чи середовищі.

---

## 📂 Структура
```bash
app/assets/fonts/
├── 📘 README.md
├── Roboto-Bold.ttf            # рекомендований жирний шрифт
└── RobotoMono-Regular.ttf     # рекомендований моноширинний шрифт
```

---

## 🧭 Призначення
- Гарантувати наявність fallback-шрифтів для генераторів таблиць/зображень.
- Використовуватися `FontService` у випадках, коли системні шляхи недоступні.
- Служити місцем для шрифтів, які розповсюджуються разом із репозиторієм.

---

## 🧱 Рекомендовані файли
- **`Roboto-Bold.ttf`** — базовий жирний шрифт для підписів.
- **`RobotoMono-Regular.ttf`** — моноширинний шрифт для таблиць/кодів.

Якщо цих файлів немає:
- `FontService` спробує знайти шрифти в системних каталогах (Linux/macOS/Windows).
- Якщо не знайде — використає дефолтний шрифт Pillow (`ImageFont.load_default()`), що може вплинути на зовнішній вигляд.

---

## ⚙️ Конфігурація
```yaml
image_generation:
  font_paths:
    bold:
      - "/opt/fonts/YourBold.ttf"
      - "app/assets/fonts/Roboto-Bold.ttf"
    mono:
      - "/opt/fonts/YourMono.ttf"
      - "app/assets/fonts/RobotoMono-Regular.ttf"
```

---

## ✅ Примітки
- Зберігайте лише ліцензійні шрифти або ті, що дозволені для розповсюдження.
- Якщо додаєте новий шрифт, оновіть список у цьому README та в конфігах (`90_image_generation.yaml`).
