# 🧰 app/shared/__init__.py
"""
🧰 Пакет `shared` — крос-функціональні компоненти застосунку.

🔹 Основні підпакети:
    • `cache/` — асинхронний LRU+TTL кеш HTML.
    • `metrics/` — метрики Prometheus та експортер `/metrics`.
    • `prompts/` — текстові шаблони для LLM (українські, OCR).
    • `utils/` — утиліти та сервіси (логування, URL-стратегії, PromptService).

📌 Призначення:
    • Централізація спільних сервісів та інструментів.
    • Зберігання текстових промтів поза кодом.
    • Мінімізація дублювання між шарами (`bot`, `domain`, `infrastructure`, `config`).

⚙️ Принципи:
    • SRP (Single Responsibility Principle).
    • KISS (Keep It Simple, Stupid).
    • Чиста архітектура з чіткими межами між шарами.
"""
