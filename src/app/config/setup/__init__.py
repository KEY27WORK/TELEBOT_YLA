# 🧰 app/config/setup/__init__.py
"""
🧰 setup — пакет складання та ініціалізації застосунку.

Містить:
  • constants.py — типобезпечні константи (UI/LOGIC/CALLBACKS) і legacy-обгортку generate_menu_pattern()
  • container.py  — DI‑контейнер, що створює всі сервіси/обробники у коректному порядку
  • bot_registrar.py — централізована реєстрація обробників у telegram.ext.Application

Правила:
  • Цей пакет не містить бізнес‑логіки — лише «складання» та «проводка».
  • Імпорти з цього пакета стабільні та можуть використовуватися верхніми шарами.
"""

# 🧩 Внутрішні модулі проєкту
from .constants import AppConstants, CONST, generate_menu_pattern  # 🧷 Публічні константи (legacy API)
from .bot_registrar import BotRegistrar                            # 🧾 Реєстратор хендлерів

# ================================
# 🔓 ПУБЛІЧНИЙ API ПАКЕТА
# ================================
__all__ = [																				# 📢 Явно експонуємо стабільні символи пакета
    "AppConstants",																		# 🧷 Набір структурованих констант (enum‑подібний інтерфейс)
    "CONST",																			# 🧱 Плоскі константи для швидкого доступу/сумісності
    "generate_menu_pattern",															# 🧵 Legacy‑функція для побудови regex‑патернів меню
    "Container",																		# 📦 Точка входу до DI‑контейнера
    "BotRegistrar",																		# 🧾 Централізована реєстрація хендлерів
]


def __getattr__(name: str):
    if name == "Container":
        from .container import Container  # локальний імпорт, щоб уникнути циклів
        return Container
    raise AttributeError(f"module {__name__!r} has no attribute {name!r}")
