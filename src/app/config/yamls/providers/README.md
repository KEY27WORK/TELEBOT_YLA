# 🚚 app/config/yamls/providers
"""
🚚 Конфіги інтеграцій із зовнішніми провайдерами.

🔹 Завантажуються після `base/` і `features/`, щоб задати окремі тарифи/ключі
🔹 Призначені для post-processing над глобальними параметрами (наприклад, доставка)
🔹 Можуть бути override-нуті `local/`, якщо потрібні секрети чи тестові значення
"""

---

## 📂 Склад каталогу

| Файл | Призначення |
| --- | --- |
| `delivery.yaml` | Тарифи Meest/служб доставки. Ключі (`us`, `uk`, `germany`, `poland`) мають збігатися з `pricing.regional_costs.*.country_code`. |

---

## 🧭 Примітки
- Якщо додаєте іншого провайдера (наприклад, SMS або AI-сервіс), створіть `XX_provider.yaml` і задокументуйте його тут.
- Значення відсюди все ще можуть бути перекриті локальними YAML-ами (`local/`), якщо треба кастомізувати середовище.
