📋 Инструкция для Open Interpreter — Тесты AvailabilityManager и парсинга JSON-LD

---

Задача:  
Написать юнит-тесты для класса AvailabilityManager и для парсера JSON-LD (JsonLdAvailabilityParser).  

Основные требования:  
1. Тесты должны быть на Python с использованием unittest или pytest.  
2. Для AvailabilityManager проверить:  
  - Метод check_and_aggregate — возвращает корректно отформатированный текст наличия цветов и размеров по регионам.  
  - Метод check_simple_availability — возвращает строку с эмодзи статусов (✅/❌) для каждого региона и всегда "🇺🇦 - ❌".  
  - Внутренние методы _aggregate_availability и _check_region_simple должны корректно вызывать парсер и обрабатывать результат.  
3. Для JsonLdAvailabilityParser проверить:  
  - Правильный парсинг JSON-LD данных из HTML.  
  - Корректную работу fallback-парсинга цветов из HTML, если JSON-LD пустой.  
  - Нормализацию размеров (_map_size) — преобразование "XLarge" → "XL" и др.  
4. Использовать мокирование (mock) для имитации HTML страниц и данных JSON-LD, чтобы не делать реальные HTTP запросы.  
5. Проверить граничные случаи — пустой HTML, ошибки JSON, отсутствие цветов и размеров.  
6. Проверить корректность объединения и форматирования данных для вывода.  

Структура тестов:  
- tests/parsing/test_availability_manager.py  
- tests/parsing/test_json_ld_parser.py  

Дополнительно:  
- Тесты должны быть изолированы, не зависеть от внешних сервисов.  
- Использовать асинхронные тесты для async методов (например, с pytest-asyncio).  
- Логгирование можно заглушить или проверить вызовы при необходимости.  

---

Просьба сгенерировать полный набор тестов по этим критериям на Python с комментариями для каждого теста.