# 🔌 infrastructure/adapters
Адаптери між інфраструктурними сервісами та доменними контрактами. Забезпечують «чисті» API без Telegram/UI залежностей і допомагають поступово мігрувати код на нові інтерфейси.

---

## 📂 Структура
```
adapters/
├── 📘 README.md
├── 📄 __init__.py
├── 📄 hashtag_adapter.py
└── 📄 price_facade.py
```

---

## 🧭 Призначення
- Узгоджувати старі та нові інтерфейси без переписування всього модуля.
- Приховувати специфіку Telegram/бота за простим фасадом, яким легко користуватися в інших сервісах.
- Спрощувати тестування: адаптери мокаються як звичайні класи.
- Дотримуватися DIP — домен залежить від контрактів (`Protocol`), а не від конкретних реалізацій.

---

## 🧩 Ключові компоненти
- **`hashtag_adapter.py`** — `HashtagGeneratorStringAdapter` конвертує сучасний `IHashtagGenerator` (множина хештегів) у legacy-рядок (`"tag1 tag2"`).  
- **`price_facade.py`** — `PriceMessageFacade` та `IPriceMessageFacade`, які делегують у `PriceCalculationHandler`, повертаючи `(ProductInfo, message, images)` без Telegram API.  
- **`__init__.py`** — експортує публічний API модуля (обидва адаптери та фасадний контракт).

---

## 🚀 Приклад використання
```python
from app.infrastructure.adapters import (
    HashtagGeneratorStringAdapter,
    PriceMessageFacade,
)
from app.infrastructure.content.hashtag_generator import HashtagGenerator
from app.bot.handlers.price_calculator_handler import PriceCalculationHandler

hashtag_adapter = HashtagGeneratorStringAdapter(HashtagGenerator(...))
hashtags_str = await hashtag_adapter.generate(product_info)

price_facade = PriceMessageFacade(handler=PriceCalculationHandler(...))
product, message, images = await price_facade.calculate_and_format(url)
```

---

## ✅ Примітки
- README тримаємо синхронізованим зі списком файлів; додаєш новий адаптер — онови розділ «Ключові компоненти».  
- Усі адаптери логують власну ініціалізацію та виклики (`LOG_NAME.*`), що полегшує дебаг.  
- Тут немає конфігурації — адаптери працюють поверх уже налаштованих сервісів (PromptService, PriceCalculationHandler тощо).
