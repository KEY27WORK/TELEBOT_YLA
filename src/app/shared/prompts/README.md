# 🧠 Шаблони Промтів

Ця директорія містить усі текстові шаблони, що використовуються для генерації запитів до OpenAI (через GPT-4o/ChatGPT).

📌 Шаблони зберігаються окремо від коду для зручності редагування, перевикористання та підтримки чистої архітектури.

## 📂 Структура

- `uk/` — україномовні шаблони для генерації:
  - `music.txt`, `hashtags.txt`, `slogan.txt`, `weight.txt`, `clothing_type.txt`, `translation.txt`
- `ocr/` — шаблони та приклади для розпізнавання таблиць розмірів:
  - `base.txt` — основний текстовий шаблон
  - `example_unique.json`, `example_general.json` — приклади у форматі JSON

## ⚙️ Використання

- Шаблони динамічно завантажуються через модуль `app.shared.utils.prompt_loader`.
- Форматування та підстановка параметрів — через `prompts.py` і `prompt_service.py`.
- Файли кешуються в памʼяті (`@lru_cache`) для підвищення продуктивності.

## ✏️ Редагування

Ці шаблони можна змінювати або доповнювати **без змін у Python-коді**.  
Це дозволяє легко оновлювати логіку генерації (наприклад, змінювати інструкції до GPT) без залучення розробника.

📎 Приклад використання:  
Промт `slogan.txt` використовується для створення слогану в `PromptService.get_slogan_prompt()`.
