# 🔎 Пошукові парсери — `product_search/`

Підмодуль для **UI‑пошуку** товарів на YoungLA за артикулом або назвою.

## 📂 Структура
```bash
📦 product_search/
├── 📘 README.md          # (цей файл) опис підмодуля
├── 📄 __init__.py        # експортує ProductSearchResolver
└── 📄 search_resolver.py # реалізація UI-пошуку через Playwright
```

## 📦 Клас: `ProductSearchResolver`
Реалізує контракт `IProductSearchProvider`:
- `resolve_one(query: str) -> Optional[Url]` — перший релевантний товар.
- `resolve_many(query: str, limit: int) -> List[SearchResult]` — до `limit` товарів.

### Вхід / Вихід
- Вхід: `query: str`, DI‑сервіси (`UrlParserService`, `ConfigService` — опційно).
- Вихід: абсолютні канонізовані URL у форматі доменних типів.

### Поведінка
1. Відкриває головну сторінку.
2. Вмикає пошуковий діалог та вводить `query`.
3. Пробує зібрати посилання з predictive‑підказок.
4. Якщо нема — переходить на повну сторінку результатів і бере перші лінки.
5. Канонізує, повертає список/один результат.

### Рекомендації
- Використовуйте headless + невеликі паузи між запитами.
- Кешуйте найпопулярніші артикули.
