# 🏠 app/config/yamls/local
"""
🏠 Локальні оверрайди для розробників/оточень.

🔹 Підвантажуються останніми і перекривають `base/`, `features/`, `providers/`
🔹 Використовуйте для секретів, токенів, тимчасових порогів, кастомних URL
🔹 Не комітьте реальні значення: тримайте файли у `.gitignore`
"""

---

## 📂 Структура

| Файл | Призначення |
| --- | --- |
| `99_local.yaml` | Приклад локального YAML. Можна копіювати/переіменовувати (`99_username.yaml`) для своїх overrides (API ключі, тайм-аути, тестові URL, фічі в розробці). |

---

## 🧭 Правила використання
- Додайте усі локальні файли у `.gitignore`, щоб не відправити секрети в репозиторій.
- Використовуйте `%ENV%` (`${VAR}`) там, де це можливо — так легше тримати локальні файли короткими.
- Якщо потрібен спільний override для певного середовища (наприклад, stage), краще створити окремий каталог (наприклад, `providers/stage/`) замість локального файлу.
