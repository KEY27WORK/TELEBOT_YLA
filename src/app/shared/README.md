# 🧰 Модуль `shared`

Цей модуль містить спільні компоненти, які використовуються в усіх шарах архітектури проєкту (`bot`, `infrastructure`, `domain`, `config`). Він об'єднує утиліти, сервіси, текстові шаблони та логіку, яка не належить до конкретного доменного контексту.

> 📌 Принципи:
> - Повна ізоляція загальної логіки
> - Без залежності від сторонніх модулів проєкту
> - Відповідає SRP та KISS

---

## 📦 Структура

### `utils/` — Утиліти та сервіси

Містить логіку, яка забезпечує базову функціональність:

- `logger.py` — централізована система логування (консоль + файл)
- `url_parser_service.py` — сервіс розбору та аналізу URL-адрес
- `prompt_loader.py` — завантаження текстових шаблонів промтів з диску
- `prompts.py` — обгортка для raw-промтів: повертає шаблони у вигляді рядків.

> ❗ Усі утиліти побудовані як незалежні сервіси з мінімальними залежностями.
> Вони використовуються через DI (контейнер).

---

### `prompts/` — Текстові шаблони для OpenAI

Ця директорія містить лише контент — текстові файли, що є шаблонами для генерації запитів до GPT.

#### uk/

Містить україномовні шаблони для:

- `music.txt` — генерація музичних рекомендацій
- `hashtags.txt` — генерація хештегів
- `slogan.txt` — генерація слоганів
- `weight.txt` — оцінка ваги товару
- `translation.txt` — переклад опису
- `clothing_type.txt` — визначення типу одягу

> 🏷️ Файли мають просту структуру з `{}`-плейсхолдерами, які підставляються через `.format()`.

#### ocr/

Асети для розпізнавання таблиць розмірів:

- `base.txt` — базовий шаблон OCR-промта
- `example_unique.json` — приклад таблиці для `ChartType.UNIQUE`
- `example_general.json` — приклад таблиці для `ChartType.GENERAL`

> 📌 Ці файли використовуються в `get_size_chart_prompt()` як частина OpenAI-запиту.

---

## ✅ Призначення

Модуль `shared`:

- Забезпечує загальні сервіси для всього застосунку
- Розділяє код і контент (шаблони промтів)
- Дотримується чистої архітектури (окремі відповідальності)

Цей модуль є безпечним для повторного використання і не має зовнішніх залежностей від внутрішньої логіки бота.

---

## 📚 Приклад використання

```python
from app.shared.utils import PromptService

prompt_service = PromptService()
prompt = prompt_service.get_slogan_prompt(title="4044 Gladiator", description="Тканина, посадка, вайб...")
