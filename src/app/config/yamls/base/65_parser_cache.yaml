# ⚙️ app/config/yamls/base/65_parser_cache.yaml
# 🧊 Кеш HTML для парсерів
# Дає змогу повторно використовувати DOM між запитами (ParserFactory, CollectionProcessing).

# ================================
# 💾 LRU-КЕШ
# ================================
parser:
  html_cache:
    enabled: true                    # 🔛 Вмикаємо/вимикаємо кеш
    ttl_sec: 300                     # ⏳ Час життя запису (сек)
    max_entries: 256                 # 📦 Розмір LRU
    key_strategy: "url"              # 🗝️ "url" або "url+region" при потребі
