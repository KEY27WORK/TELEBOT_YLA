# ⌨️ keyboards — Reply/Inline меню Telegram
Пакет **`app/bot/ui/keyboards`** генерує всі клавіатури бота (Reply та Inline), кешує їх та централізує доступ до текстів/`callback_data`.

---

## 📂 Структура
```bash
keyboards/
├── 📘 README.md
├── 📄 __init__.py
└── 📄 keyboards.py
```

---

## 🧭 Призначення
- Побудова головного Reply-меню для основних режимів бота (вставка лінків, режими, допомога тощо).
- Формування інлайн-клавіатур для керування курсами валют та показу довідки.
- Кешування готових `ReplyKeyboardMarkup` / `InlineKeyboardMarkup`, щоб зменшити кількість алокацій.
- Підтримка зворотної сумісності через статичні обгортки, що використовують глобальний `CONST`.

---

## 🧩 Компоненти
- **`__init__.py`** — експортує фасад `Keyboard` для зручного імпорту (`from app.bot.ui.keyboards import Keyboard`).
- **[`keyboards.py`](./keyboards.py)** — містить клас `Keyboard`:
  - приймає `AppConstants` через конструктор (DI) і використовує `UI.REPLY_BUTTONS` та `UI.INLINE_BUTTONS`;
  - використовує `CALLBACKS.*.build()` для формування типобезпечних `callback_data`;
  - має методи `build_main_menu`, `build_currency_menu`, `build_help_menu` з внутрішнім кешем (`_cache_*`);
  - експонує статичні `main_menu/currency_menu/help_menu`, які створюють інстанс із глобального `CONST` для legacy-коду.

---

## ⚙️ Конфігурація та контракти
```python
from app.config.setup.constants import AppConstants, CONST

constants.UI.REPLY_BUTTONS    # тексти для кнопок Reply (INSERT_LINKS, HELP, ... )
constants.UI.INLINE_BUTTONS   # тексти для інлайн-кнопок (SHOW_RATE, HELP_FAQ, ...)
constants.UI.REPLY_PLACEHOLDER  # опційний плейсхолдер поля вводу
constants.CALLBACKS.CURRENCY_SHOW_RATE  # фабрика CallbackData
```
- DI: передавайте актуальний `AppConstants` у `Keyboard(constants)` там, де потрібні кастомні кнопки/локалізація.
- Статичні методи використовують глобальний `CONST`, тому підходять лише якщо достатньо дефолтних значень.

---

## 🚀 Приклад використання
```python
from app.bot.ui.keyboards import Keyboard

keyboard = Keyboard(app_constants)

await update.message.reply_text(
    "Оберіть дію",
    reply_markup=keyboard.build_main_menu(),
)

await update.message.reply_text(
    "Оновити курси?",
    reply_markup=keyboard.build_currency_menu(),
)
```

---

## 🧪 Тестування
- Перевіряйте розкладку Reply-клавіатури (кількість рядків, порядок кнопок).
- Стверджуйте, що інлайн-клавіатури містять коректні `callback_data` з `CALLBACKS.*`.
- Тестуйте кешування: повторні виклики мають повертати той самий об’єкт (можна порівнювати `is`).
- Для статичних методів перевірте, що використовуються значення з `CONST`.

---

## ✅ Примітки
- Нові клавіатури додавайте як методи `Keyboard`, щоб зберегти кешування та DI.
- Не будуйте `InlineKeyboardButton` з сирими рядками — використовуйте фабрики з `constants.CALLBACKS`.
- Якщо змінюються тексти кнопок у `AppConstants`, README слід актуалізувати разом із кодом.
