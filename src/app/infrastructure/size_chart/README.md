# 📐 Модуль `size_chart`

Модуль відповідає за повний цикл обробки таблиць розмірів для Telegram-бота YoungLA Ukraine: 
від завантаження та OCR — до візуального формування PNG-таблиці.

> 📌 Принципи:
> - Розділення відповідальностей (SRP)
> - Без логіки бота — лише інфраструктура
> - Підтримка різних типів таблиць: General, Unique, Grid
> - Побудова за DI-принципом (FontService, SizeChartService)

---

## 📦 Структура

### `size_chart/` — кореневий модуль

- `image_downloader.py` — Завантаження таблиць з веб-сайтів
- `ocr_service.py` — Обробка зображень через OCR (tesseract/AI)
- `size_chart_service.py` — Оркестратор: обʼєднує загрузку, розпізнавання, генерацію

> ⚙️ Усі сервіси асинхронні, підтримують логування.

---

### `generators/` — Візуальні генератори таблиць

Містить класи для створення PNG-таблиць різного типу:

- `base_generator.py` — абстрактний клас з базовою логікою (шрифт, малювання)
- `general_table_generator.py` — класичні таблиці (розмір → параметри)
- `unique_table_generator.py` — адаптивні таблиці (параметри → розміри)
- `unique_grid_table_generator.py` — сіткові таблиці (зріст × вага → розмір)

> 🧩 Використовують `FontService` і `TableGeometryService`.

---

### `services/` — Сервіси для розрахунку геометрії

- `table_geometry_service.py` — логіка масштабування, розрахунку розмірів, позиціонування клітинок

> 🧠 Не залежить від PIL чи ImageDraw, працює лише з цифрами.

---

## ✅ Призначення

Модуль `size_chart`:

- Використовується в AI-режимі Telegram-бота
- Генерує красиві таблиці на основі JSON/текстових описів
- Може працювати автономно без інших частин бота
- Забезпечує підтримку масштабування під будь-який формат

---

## 📚 Приклад використання

```python
from app.infrastructure.size_chart import SizeChartService

size_chart_service = SizeChartService(
    font_service=font_service, 
    ocr_service=ocr_service, 
    image_downloader=image_downloader
)

output_path = await size_chart_service.generate_size_chart(image_url)
