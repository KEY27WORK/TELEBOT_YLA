# 🧰 Пакет `utils`

Ця директорія містить ключові утиліти та сервіси, що є **фундаментом** для багатьох операцій у проєкті. Вони розроблені для повторного використання та дотримуються принципів **чистої архітектури**.

---

## 📦 Основні компоненти

### `logger.py`

- **Призначення**: Налаштовує централізовану систему логування для всього проєкту.
- **Ключова функція**:  
  `setup_logging()` — ініціалізує логер на основі конфігурації з `config.yaml`, дозволяючи гнучко керувати:
  - рівнем логування
  - виводом у консоль
  - фільтрацією сторонніх бібліотек

---

### `url_parser_service.py`

- **Призначення**: Єдиний сервіс для всієї логіки, пов'язаної з URL-адресами YoungLA.
- **Ключові функції**:
  - Визначення типу URL (товар / колекція)
  - Витяг регіону та валюти з посилання
  - Побудова повних URL на основі конфігурації (`config.yaml`)
- 🔄 Повністю замінює застарілі утиліти `region_utils` та `url_utils`.

---

### `prompt_loader.py`

- **Призначення**: Низькорівневий завантажувач шаблонів.
- **Ключові функції**:
  - `load_prompt()` — завантажує локалізовані текстові шаблони з `shared/prompts/uk/`
  - `load_ocr_asset()` — завантажує JSON-приклади та шаблони для OCR (`shared/prompts/ocr/`)
- 🧠 Підтримує кешування та багатомовність.

---

### `prompts.py`

- **Призначення**: "Конструктор" промтів.
- **Ключові функції**:
  - `get_prompt()` — повертає форматований промт за типом (`PromptType`)
  - `get_size_chart_prompt()` — збирає OCR-промт з шаблону + прикладу
- 🧩 Використовує `prompt_loader` для завантаження шаблонів.

---

## 🧩 Висновок

Модуль `utils` — це основа для спільної інфраструктури, яка забезпечує стабільну роботу логування, обробки URL та генерації промтів у Telegram-боті та за його межами.
