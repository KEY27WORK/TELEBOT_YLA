# Інструкція по додаванню нової фічі в TELEBOTYLAUKRAINE

1. **Розбір архітектури**
   - Прочитай README на рівні `src/app` (особливо `bot`, `domain`, `infrastructure`, `config`) — там описано ролі кожного шару.
   - Глянь `src/app/config/setup/container.py` — там зібраний DI, видно, які сервіси вже є, як вони інʼєктяться.

2. **Точка входу**
   - `src/app/bot/handlers/link_handler.py` — роутер для текстів/URL.
   - Якщо нова поведінка викликається по URL, починай звідси: перевіряй, чи попав запит під умову, викликай сервіс, заверши `return`.

3. **Визнач шару логіки**
   - **domain/** — чисті Protocol/DTO (контракти). Нові логічні інтерфейси додавай сюди.
   - **infrastructure/** — конкретні реалізації (парсери, AI, WebDriver, сервіси).
   - **bot/** — UI-орієнтовані обробники, месенджери, static messages.
   - Нова функціональність має використовувати існуючі сервіси, не дублюй HTTP/AI/парсери.

4. **План дій**
   1. Опиши, що робить користувач → як UX повинен виглядати.
   2. Розшифруй, які дані потрібні та в яких сервісах їх брати.
   3. Додай/розшир `infrastructure` (новий BannerDropService або розширення парсера), поки логіка залишиться в цьому шарі.
   4. Зареєструй у контейнері: створи екземпляр, передай залежності (WebDriver, AI, CollectionHandler тощо).
   5. Пробрось у потрібний хендлер (`LinkHandler`, `CollectionHandler`), онови `static_messages`, якщо потрібен новий UX.
   6. Використовуй `ImageSender`, `CollectionHandler`, `AITaskService`— нічого не переписывай по-напрежению.

5. **UI та повідомлення**
   - Якщо потрібні нові тексти — додавай їх у `src/app/bot/ui/static_messages.py` з тим же стилем (емоджі + короткий опис).
   - Перевір, що формат повідомлень (HTML/Markdown) використовує `MessageFormatter` і `ImageSender` для медиагруп.

6. **Документація**
   - Якщо створюєш нову папку, зроби `README.md` з коротким описом (що за сервіс, які публічні методи).
   - Не забудь оновити `infrastructure/services/README.md` або аналогічні README, якщо додаєш рівень сервісів.

7. **Тестування**
   - Розглянь додавання unit-тесту для нового сервісу (наприклад, мок `WebDriverService`, `AITaskService`).
   - Якщо тестів немає — хоча б ручний запуск сценарію (отправить https://www.youngla.com).
   - Перевір логування (`logs/`), щоб бачити ключові кроки (fetch, send media group, parse collection).

8. **Після змін**
   - Перевір `git status`, відповідні README, додай файл гайд по комітам (якщо потрібно).
   - Придержуйся Conventional Commits с эмоджі для каждого коміта (`✨ feat: ...`, `🐛 fix: ...`).
   - Нарешті, пуш у remote після визову `git remote -v` і `git push origin main`.
