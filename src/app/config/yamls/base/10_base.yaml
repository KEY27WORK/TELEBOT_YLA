# ⚙️ app/config/yamls/base/10_base.yaml
# 🧰 Базові налаштування застосунку
# Визначає дефолти вебдрайвера, OpenAI, ALT-text, parser images та інші глобальні параметри.

# ================================
# 🛠️ СИСТЕМНІ ДЕФОЛТИ
# ================================
webdriver_path: "./chromedriver"                 # 🧭 Шлях до ChromeDriver
default_language: "uk"                           # 🗣️ Мова UI та повідомлень
max_retries: 5                                   # 🔁 Кількість повторів інфра-операцій
collection_processing_delay_sec: 2               # ⏱️ Пауза між колекційними запитами
collection_progress_update_interval: 5           # 📊 Як часто показувати прогрес

# ================================
# 🤖 OPENAI (AI-сервіси)
# ================================
openai:
  api_key: "${OPENAI_API_KEY}"                   # 🔐 Ключ із ENV
  model: "gpt-4o-mini"                           # 🧠 Модель за замовчуванням
  defaults:                                      # ⚙️ Базові параметри виклику
    temperature: 0.30                            # 🌡️ Загальна «креативність» відповідей
    max_tokens: 1024                             # 🔢 Межа токенів для універсальних промптів
  prompts:                                       # 🎯 Профілі для різних сценаріїв
    slogan:
      temperature: 0.70                          # 📣 Більше креативності для лозунгів
      max_tokens: 64                             # 📏 Короткі відповіді
    music:
      temperature: 0.50                          # 🎵 Баланс креативу для музичних порад
      max_tokens: 512                            # 📦 Середній розмір відповіді
    translation:
      temperature: 0.30                          # 🌐 Ближче до дослівності
      max_tokens: 512
    weight:
      temperature: 0.30                          # ⚖️ Консервативні оцінки ваги
      max_tokens: 16                             # ✂️ Вистачає короткої відповіді
    clothing_type:
      temperature: 0.20                          # 👕 Мінімум імпровізації
      max_tokens: 16
    size_chart:
      temperature: 0.00                          # 📐 Тільки структуровані дані
      max_tokens: 2048                           # 🧾 Довгі таблиці
  cache:                                         # 🧊 Кеш AI-перекладів
    enabled: true                                # 🔛 Вмикаємо кешування відповіді
    ttl_hours: 720                               # ⏳ 30 діб
    max_items: 1000                              # 📦 Ліміт записів у LRU
    persist_dir: ".cache/ai_translations"        # 💾 Куди зберігаємо диск-кеш

  # ================================
  # 💰 Тарифи моделей (для телеметрії / аналітики)
  # ================================
  prices:                                        # 💰 Телеметрія вартості моделей
    gpt-4o-mini:
      input_per_1k: 0.15                     # 💸 $/1K input токенів
      output_per_1k: 0.60                    # 💸 $/1K output токенів
    gpt-4o:
      input_per_1k: 5.00
      output_per_1k: 15.00
    gpt-4-turbo:
      input_per_1k: 10.00
      output_per_1k: 30.00

# ================================
# 🖼️ ALT-тексти для зображень (IMP-042)
# ================================
alt_text:
  concurrency: 2                                   # 🚦 Паралельні запити до AltTextGenerator
  cache:                                           # 🧊 Кеш HTML → ALT
    ttl_sec: 86400                                 # ⏳ Живе 1 добу
    max_entries: 2048                              # 📦 Максимальна кількість записів

# ================================
# 🖼️ Парсер: фільтри зображень (IMP-018)
# ================================
parser:
  images:                                         # 🧩 Глобальні правила відбору зображень
    allowed_ext: [".jpg", ".jpeg", ".png", ".webp", ".avif"]
    bad_tokens: ["sprite", "favicon", "logo", "icon", "spinner", "loading", "placeholder", "badge", "swatch", "thumb", "minicart", "lazy"]
    min_side_px: 120                              # 📐 Мінімальна сторона прев'ю

# ================================
# 🔍 Пошук (IMP-030)
# ================================
search:
  goto_timeout_ms: 30000                          # ⏳ Таймаут на перехід сторінки
  idle_timeout_ms: 15000                          # 😴 Очікування без активності
  predictive_timeout_ms: 7000                     # 🤖 Таймаут прогнозних запитів

  max_results_default: 10                         # 📄 Дефолтний ліміт результатів
  max_results_hardcap: 30                         # 🚧 Жорстка верхня межа

  retry_attempts: 2                               # 🔁 Повтори пошукового запиту
  retry_backoff_ms: 600                           # ⏱️ Backoff між повторами

# ================================
# 📏 SizeChart — градієнтні ліміти паралелізму (IMP-047)
# ================================
size_chart:
  concurrency:
    download:
      max: null                                  # 🚦 Автотюн, null = без верхньої межі
      min: 2                                     # 📥 Мінімум потоків завантаження
      max_cap: 16                                # ⛔ Жорстка стеля
    ocr:
      max: null
      min: 1
      max_cap: 8
    autotune:
