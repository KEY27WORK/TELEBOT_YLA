# 🧾 OCR Prompts

Каталог **`app/shared/prompts/ocr`** містить текстові шаблони та приклади JSON,
які використовуються для генерації промптів розпізнавання таблиць розмірів.
Викликається через `PromptService.get_size_chart_prompt(...)`.

## 📄 Файли

- `base.txt` — основний шаблон промпту:
  - описує правила формату (лише валідний JSON, числові значення з крапкою),
  - визначає вимоги до перекладу назв колонок українською,
  - задає структуру відповіді ("Title", "Розмір", параметри).
- `example_general.json` — приклад для загальної таблиці з діапазонами значень.
- `example_unique.json` — приклад компактної таблиці з одиничними значеннями.

```bash
🧾 ocr/
├── 📘 README.md
├── 📄 base.txt
├── 📄 example_general.json
└── 📄 example_unique.json
```

## 🧭 Потік використання

1. `PromptService.get_size_chart_prompt(chart_type)` читає `base.txt`.
2. Додає `extra_conditions` залежно від `ChartType` (general/unique).
3. Підставляє відповідний JSON (`example_general.json` чи `example_unique.json`).
4. Отриманий текст передається в LLM/OCR, яке повертає нормалізований JSON.

## 🛡️ Принципи

- **Строгий формат** — жодного тексту поза JSON.
- **Конверсія одиниць** — дюйми конвертуються у сантиметри.
- **Переклад заголовків** — коротко українською (1–2 слова).
- **Сумісність** — приклади синхронізовані з парсером, щоб валідатори приймали структуру.

## 🧪 Рекомендації щодо тестування

- Перевірити, що шаблон коректно підставляє `extra_conditions` і `example_json`.
- Імітувати відповіді LLM, перевіряючи відповідність правилам (`decimal`, `null`, довжина масивів).
