# ⚙️ Config — Конфігурація застосунку

Цей пакет відповідає за **всю конфігурацію та ініціалізацію** застосунку.  
Він зберігає ключі доступу, базові налаштування та управляє залежностями через **DI-контейнер**.

---

## 🗂 Структура

📦 **config**  
┣ 📂 **setup** — налаштування та "збірка" застосунку перед запуском  
┃ ┣ 📜 **container.py** — головний **DI-контейнер**, створює та пов’язує сервіси й обробники  
┃ ┣ 📜 **bot_registrar.py** — автоматично реєструє всі хендлери та фічі  
┃ ┗ 📜 **constants.py** — текстові константи для меню та патерни  
┣ 📜 **config_service.py** — єдиний доступ до налаштувань і конфігурацій  
┣ 📜 **config.yaml** — базова конфігурація регіонів і валют  
┣ 📜 **config.json** — локальні налаштування (webdriver, мова, retries)  
┣ 📜 **weights.json** — ваги товарів (для розрахунку доставки)  
┗ 📜 **README.md** — цей файл

---

## 🧩 Основні компоненти

### **ConfigService**
Сервіс, що:
- Завантажує API-ключі з `.env`
- Отримує курс валют через API НБУ (з кешем і fallback)
- Керує локальними JSON-файлами (`weights.json`, `product_types.json`)
- Працює як **Singleton** — один інстанс на весь процес

### **Container (DI-контейнер)**
- Створює та зберігає всі сервіси та обробники у правильному порядку
- Забезпечує **Dependency Injection** — жодних "жорстких" імпортів у бізнес-логіці

### **Bot Registrar**
- Автоматично реєструє всі фічі та глобальні обробники бота
- Використовується **main.py** при старті

---

## 🚀 Використання

### Отримати доступ до токенів або конфігів:
```python
from app.config import ConfigService

config = ConfigService()
print(config.telegram_token)

Використати контейнер:

from app.config import container

app_container = container
link_handler = app_container.link_handler


⸻

💾 Файли конфігурації
	•	config.yaml — базові налаштування регіонів і валют:

regions:
  us:
    base_url: "https://www.youngla.com"
    currency: "USD"


	•	config.json — локальні технічні налаштування:

{
    "webdriver_path": "./chromedriver",
    "default_language": "uk",
    "max_retries": 5
}


	•	weights.json — локальна база ваг:

{
    "6022 - carpenter jeans": 1.2,
    "w557 classic crewneck": 0.5
}
