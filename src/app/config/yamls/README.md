# 📚 app/config/yamls
"""
📚 Дерево YAML-конфігів, яке читає `ConfigService`.

🔹 `base/` → дефолтні значення (завжди завантажуються першими)
🔹 `features/`, `providers/` → тематичні overrides поверх base
🔹 `local/` → локальні/секретні оверрайди з найвищим пріоритетом
"""

---

## 🗺️ Каталоги

| Каталог | Призначення |
| --- | --- |
| `base/` | Базові конфіги застосунку. Регулюють регіони, OpenAI, parser selectors, ALT, OCR, pricing, availability, Playwright, файли, metrics, logging, flags. |
| `features/` | Параметри конкретних фіч (музика, image generation, Telegram HTTP). |
| `providers/` | Інтеграції з зовнішніми постачальниками (наприклад, тарифи Meest). |
| `local/` | Локальні overrides для розробників/оточень; читаються останніми. |

> Кожен підкаталог має власний README з деталями про файли (див. `base/README.md`, `features/README.md` тощо).

---

## ⏱️ Порядок завантаження
Конфіги зчитуються у такій послідовності:

1. `base/**.yaml`
2. `features/**.yaml`
3. `providers/**.yaml`
4. `local/**.yaml` (найвищий пріоритет)

Всередині каталогу порядок визначається іменами файлів (`00_`, `10_`, `15_` ...), тож додаючи новий блок, використовуйте числовий префікс.

---

## 🧭 Поради
- Секрети тримайте в ENV або у файлах `local/`, які занесені в `.gitignore`.
- Якщо з’являється новий великий модуль — створюйте окремий підкаталог (наприклад, `providers/sms/`).
- Не дублюйте ті ж ключі у різних каталогах без потреби: base → загальні дефолти, features/providers → цільові overrides.
