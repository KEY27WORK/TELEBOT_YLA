# 🧠 Модуль: Парсери продуктів

Цей модуль відповідає за **всю логіку парсингу** сторінок товарів, колекцій і результатів пошуку на сайті YoungLA.

## 🔍 Призначення

- Завантаження HTML-сторінок і витяг структурованих даних (назва, опис, ціна, зображення, наявність тощо).
- Пошук товарів за артикулом або назвою.
- Уніфікований інтерфейс для подальшої обробки (через `ProductHandler`, `CollectionHandler` тощо).

## 📦 Основні компоненти

### `base_parser.py` — головний оркестратор
- Відповідає за повний цикл обробки сторінки товару.
- Делегує витяг даних обʼєкту `HtmlDataExtractor`.
- Приймає рішення щодо fallback логіки, зокрема для stock.

### `html_data_extractor.py` — екстрактор даних
- Низькорівнева утиліта, що вміє **витягувати** дані з DOM (JSON-LD, legacy).
- **Не містить логіки вибору** джерела — цим займається `BaseParser`.

### `parser_factory.py` — фабрика створення
- Вміє створювати інстанси парсерів (`BaseParser`, `UniversalCollectionParser`, тощо).
- **Не приймає рішення**, який саме парсер потрібен. Цим займається логіка вищого рівня (наприклад, `LinkHandler`).

### `collections/universal_collection_parser.py`
- Парсить колекції товарів (наприклад, `/collections/summer-drop`).
- Витягує всі посилання на товари з колекції.

### `product_search/search_resolver.py`
- Реалізує пошук товару за назвою або артикулом.
- Повертає перший релевантний результат для подальшого парсингу.
- Експортує клас `ProductSearchResolver`.

## 🧱 Архітектура

- Парсери поділено на підпапки:
  - `collections/` — логіка парсингу колекцій
  - `product_search/` — логіка пошуку товарів
  - `products/` — резерв для спеціалізованих парсерів (на майбутнє)

## ✅ Принципи

- SOLID, DI (всі сервіси передаються через конструктори)
- Розділення обов’язків: оркестратор (BaseParser) vs екстрактор (HtmlDataExtractor)
- Мінімум звʼязності між компонентами
- Гнучка інтеграція з Telegram-ботом через інтерфейси `IProductDataProvider`
