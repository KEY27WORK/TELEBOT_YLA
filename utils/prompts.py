""" 📋 prompts.py — зберігає текстові шаблони для OpenAI та інших сервісів.

🔹 Функції:
- `get_prompt()` — повертає промт за назвою з підставленими параметрами.
- `get_size_chart_prompt()` — повертає промт для таблиць розмірів за типом.

🔹 Використовується для:
- генерації контенту (музика, хештеги, слогани)
- перекладу описів товарів
- визначення ваги та типу одягу
- розпізнавання таблиць розмірів (OCR)
"""

# 🧱 Системні імпорти
import logging

# === 🔹 Базовий промт для таблиць розмірів (OCR) 🔹 ===
IMAGE_OCR_PROMPT = """Ти професійний OCR-фахівець, експерт із таблиць розмірів та професійний перекладач українською.
Твоє завдання – розпізнати текст із зображення, одразу перекласти його українською так, щоб звучало максимально природно і дуже коротко (1-2 слова), та подати у вигляді таблиці розмірів.

🔹 Уважно витягни всі дані із зображення.
🔹 Переклади заголовки таблиці та параметри українською.
🔹 НЕ ПЕРЕКЛАДАЙ ЛІТЕРИ РОЗМІРІВ (S, M, L, XL, XXL тощо), але перед ними додай слово "Розмір".
🔹 Перекладай слова так, щоб звучало природніше та максимально коротко (1-2 слова).
🔹 Якщо розміри зазначені у дюймах, конвертуй їх у сантиметри (1 дюйм = 2.54 см) та округли до 1 знака після коми.
🔹 Збережи структуру таблиці та переконайся, що кількість значень у кожному рядку однакова.
🔹 Поверни дані у форматі JSON.

{extra_conditions}

Приклад:
{example_json}
"""

# === 🗂️ Промти для таблиць розмірів (специфічні умови) 🗂️ ===

# 📏 Унікальні таблиці розмірів (без додаткових умов)
UNIQUE_SIZE_CHART_PROMPT = IMAGE_OCR_PROMPT.format(
    extra_conditions="Поверни лише JSON і нічого більше. Без додаткових пояснень, звітів чи тексту.",
    example_json={
        "Title": "0001 Розмірна Сітка (сантиметри)",
        "Розмір": ["S", "M", "L", "XL", "XXL"],
        "Талія": [11.1, 22.2, 33.3, 44.4, 55.5],
        "Довжина": [10.5, 20.5, 30.5, 40.5, 50.5]
    }
)

# 📋 Загальні таблиці розмірів (з додатковими умовами)
GENERAL_SIZE_CHART_PROMPT = IMAGE_OCR_PROMPT.format(
    extra_conditions="""Поверни JSON з масивами значень без вкладень та без діапазонів.
Завжди повертай JSON-формат! Якщо не можеш обробити дані, поверни порожній JSON.""",
    example_json={
        "Title": "Загальна Розмірна Сітка (сантиметри)",
        "Розмір": ["XS", "S", "M", "L", "XL"],
        "Груди": [[78.7-83.8], [83.8-88.9], [88.9-94.0], [94.0-96.5], [99.1-104.1]],
        "Талія": [[63.5-66.0], [68.6-71.1], [73.7-78.7], [81.3-86.4], [88.9-94.0]],
        "Стегна": [[86.4-91.4], [91.4-96.5], [96.5-101.6], [101.6-106.7], [106.7-114.3]]
    }
)

# === Промпти для різних задач (НЕ змінювати!) ===
PROMPTS = {
    # 🎶 Підбір музики за описом товару
    "music": """Підбери 10 треків, які ідеально пасують для товару '{title}'.
Опис: {description}.
Зображення: {image_url}.
Напиши виконавців і назви треків у форматі списку. (назви пиши без лапок)
""",

    # 🔖 Генерація хештегів
    "hashtags": """Ти експерт із SMM та Instagram-просування. 
Створи 5-7 унікальних хештегів для товару.

Опис:
Назва: {title}
Деталі: {description}

Не дублюй стандартні хештеги, запропонуй нові.
Пиши їх через пробіл.
""",

    # ⚖️ Оцінка ваги товару
    "weight": """Ти експерт із логістики та одягу. Оціни вагу товару (у кг) на основі назви, опису та фото.
Напиши тільки число, без пояснень.

🔹 Назва: {title}
🔹 Опис: {description}
🔹 Фото: {image_url}
""",

    # 🧥 Визначення типу одягу
    "clothing_type": """Ти експерт у моді та одязі. У назві товару вказано його назву.
Твоє завдання – визначити, до якого типу одягу він належить.
Відповідай одним словом, що означає тип одягу (наприклад, "jeans", "hoodie", "jacket", "shorts").

Назва товару: {title}
""",

    # 🇺🇦 Переклад опису товару українською
    "translation": """Ти професійний перекладач. Переклади текст українською, щоб звучав природно.
Розділи його на категорії: МАТЕРІАЛ, ПОСАДКА (якщо є), ОПИС, МОДЕЛЬ.
Якщо розділ відсутній у тексті — не додавай його.
Параметри моделі переклади в метричну систему.

Початковий текст: {text}
""",

    # ✨ Генерація слогану
    "slogan": """Створи короткий, стильний слоган для товару '{title}' українською мовою
у стилі бренду YoungLA. Опис товару: {description}. Максимум 10 слів.
Підбери один доречний емодзі та додай до слогану.
"""
}

# === 🛠️ Функції отримання промптів ===

def get_size_chart_prompt(chart_type: str) -> str:
    """
    📏 Отримати промт для таблиці розмірів залежно від типу.

    :param chart_type: Тип таблиці ("unique-size-chart", "general-size-chart")
    :return: Готовий промт для OpenAI
    """
    prompts = {
        "unique-size-chart": UNIQUE_SIZE_CHART_PROMPT,
        "general-size-chart": GENERAL_SIZE_CHART_PROMPT
    }
    return prompts.get(chart_type, "❌ Неизвестный тип таблицы размеров!")

def get_prompt(prompt_type: str, **kwargs) -> str:
    """
    📌 Отримати форматований промт за його типом та параметрами.

    :param prompt_type: Назва промта (music, hashtags, translation тощо)
    :param kwargs: Аргументи для форматування промту
    :return: Готовий форматований промт
    """
    prompt = PROMPTS.get(prompt_type.lower())
    if not prompt:
        raise ValueError(f"❌ Промт '{prompt_type}' не найден!")

    # 🔄 Форматуємо та захищаємо від None-значень
    return prompt.format(**{k: v if v is not None else "" for k, v in kwargs.items()})