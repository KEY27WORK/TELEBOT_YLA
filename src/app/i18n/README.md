# 🌐 app/i18n
Єдине джерело локалізацій для парсерів і Telegram-модулів. Містить YAML-словники з мапою сирих секцій → стандартизовані заголовки українською, російською та англійською мовами.

---

## 📂 Структура
```
i18n/
├── 📘 README.md
├── 📄 __init__.py
├── 📄 en.yml
├── 📄 ru.yml
└── 📄 uk.yml
```

---

## 🧭 Призначення
- Зберігати локалізовані назви секцій опису товарів, щоб `AvailabilityManager` / парсери застосовували єдині заголовки.  
- Забезпечувати простий доступ до ресурсів через `importlib.resources.files("app.i18n")`.  
- Дозволяти безболісно розширювати мапи для нових мов чи ключів.  
- Тримати Python-логіку поза словниками — YAML містить лише дані, а обробка виконується в сервісах.

---

## 🧩 Ключові файли
- **`uk.yml`**, **`ru.yml`**, **`en.yml`** — словники `key_map` з коментарями для кожного ключа (матеріал, посадка, догляд тощо).  
- **`__init__.py`** — опис пакета, пояснює, що в ньому лише YAML-файли; експортує порожній `__all__`.

---

## 🚀 Використання
```python
from importlib.resources import files
import yaml

uk_yaml = files("app.i18n").joinpath("uk.yml").read_text(encoding="utf-8")
translations = yaml.safe_load(uk_yaml)["key_map"]
human_title = translations.get(raw_section.upper(), raw_section)
```

---

## ✅ Примітки
- Усі ключі зберігаємо у верхньому регістрі (`MATERIAL`, `DESIGN`, ...).  
- Якщо додаєш нову мову, копіюй структуру `key_map` та опис у README.  
- Логування, fallback-и та валідація виконуються в сервісах, що зчитують YAML — файл повинен залишатися тільки з даними.  
- Не забувай оновлювати README та `__init__.py`, коли з’являються нові словники або секції.
