# 🖼️ Image Generation — Domain Layer

Модуль **`image_generation`** відповідає за роботу зі **шрифтами**, які використовуються під час генерації зображень (банери, підписи, превʼю для Telegram тощо).

---

## 📂 Структура

```bash
📦 image_generation
┣ 📜 __init__.py         # Експортує публічні інтерфейси
┗ 📜 interfaces.py       # Доменні контракти для роботи зі шрифтами
```

---

## 🧩 Основні компоненти

### 🔤 `FontType`
Enum, що описує доступні типи шрифтів у застосунку:
- **`BOLD`** — напівжирний (для заголовків, акцентів).
- **`MONO`** — моноширинний (для табличних або кодоподібних даних).

---

### 🖋️ `IFontService`
Доменний контракт для сервісів роботи зі шрифтами.  
Інфраструктурні реалізації зобовʼязані підтримувати:

- `get_font(font_type: FontType, size: int) -> FontLike`  
  Завантаження шрифту заданого типу та розміру.

- `get_text_width(text: str, font: FontLike) -> int`  
  Обчислення ширини тексту в пікселях (корисно для рендерингу банерів і адаптивного UI).

---

### 🪶 `FontLike`
TypeAlias, що узагальнює усі типи обʼєктів шрифтів, які повертає Pillow (`ImageFont`).

---

## ✅ Призначення модуля

- Абстрагує роботу зі шрифтами від інфраструктури (де саме зберігаються TTF-файли, як саме вони завантажуються).
- Дозволяє легко замінювати реалізацію (наприклад, локальні assets vs системні шрифти).
- Виконує **принцип DIP** (Dependency Inversion) — домен працює лише з інтерфейсами, а конкретні реалізації підключаються в шарі `infrastructure`.

---
