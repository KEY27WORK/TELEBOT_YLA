# 🧱 app/config/yamls/base
"""
🧱 Базовий шар YAML-конфігів, які завантажуються першими.

🔹 Визначає регіони, глобальні параметри, AI/парсер/ALT/metrics/flags
🔹 Служить дефолтним набором, який можна перекривати у `features/`, `providers/`, `local/`
🔹 Формує структуру ключів для `ConfigService` та DI-контейнера
"""

---

## 📂 Склад каталогу

| Файл | Призначення |
| --- | --- |
| `00_regions.yaml` | Регіони YoungLA (base URL, валюта, label) для Availability/URL parser. |
| `10_base.yaml` | Загальні налаштування: webdriver, retries, OpenAI (models, ціни, кеш), ALT-text, parser images/key map. |
| `15_parser_selectors.yaml` | CSS-селектори парсера, overrides для брендів і глобальні фільтри зображень (IMP‑015/018). |
| `20_currency.yaml` | Параметри валютного API та кешу (провайдер, TTL, API keys). |
| `25_ocr.yaml` | Таймаути/ретраї/експоненційний бекоф для OCR (OpenAI Vision) + директорія кешу. |
| `30_pricing.yaml` | Правила ціноутворення: знижки, безкоштовна доставка, `regional_costs` з `country_code`. |
| `35_sizes.yaml` | Аліаси розмірів (IMP‑056) — нормалізація значень у SizeChart/availability. |
| `40_availability.yaml` | Базові параметри Availability (TTL кешу звітів). |
| `60_playwright.yaml` | Налаштування WebDriver/Playwright (user agent, headless, delays, Cloudflare). |
| `65_parser_cache.yaml` | Конфіг HTML LRU-кеша парсерів: для повторного використання DOM (enabled, ttl, key strategy). |
| `70_files.yaml` | Шляхи до локальних файлів (weights.json, current_rate.txt, traces/ocr cache). |
| `75_metrics.yaml` | Опції метрик: тумблер Prometheus, порт експорту. |
| `80_logging.yaml` | Єдина схема логування + AI-telemetry (формати, рівні, suppress). |
| `85_flags.yaml` | Feature flags (поточний приклад — rollout v2 description extractor). |

---

## 🧭 Порядок та перекриття
- Файли пронумеровані, тому під час завантаження зливаються **за зростанням** (00 → 85).
- Будь-яке значення можна перевизначити у `features/`, `providers/` або `local/` — ці каталоги підвантажуються **після** `base/`.
- ENV (APP_*) завжди має найвищий пріоритет та перекриває YAML (див. `ConfigService`).

---

## 🧾 Поради по редагуванню
- Додавайте новий глобальний блок через окремий файл з індексом (наприклад, `55_new_module.yaml`).
- Уникайте секретів: використовуйте `${VAR}` або `${VAR:-default}` — інтерполяцію обробляє `ConfigService`.
- Пам’ятайте про наслідування: якщо конкретний бренд/ENV потребує override, краще додати його до `features/` чи `local/`, а не змінювати `base`.
