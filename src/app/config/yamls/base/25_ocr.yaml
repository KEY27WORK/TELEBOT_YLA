# 🧠 OCR (OpenAI Vision) — таймауты/ретраи/экспоненциальный бэкофф с джиттером
ocr:
  cache_dir: "var/ocr_cache"
  request_timeout_s: 60        # ⏳ таймаут на один запрос к Vision
  max_retries: 2               # 🔁 количество повторов при сбоях/timeout/JSON-ошибках
  backoff:
    base_s: 0.6                # стартовая задержка (основание экспоненты)
    cap_s: 8.0                 # максимальная задержка (cap)
    jitter: "full"             # "full" | "equal" | "none" 