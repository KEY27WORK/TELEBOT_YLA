# 📘 README-стайлгайд

Єдині правила оформлення README для всіх підпакетів `app/*`. Дотримання цього гайда гарантує, що документація залишатиметься короткою, впізнаваною та синхронізованою зі структурою каталогу.

---

## 🎯 Коли застосовуємо
- Новий модуль у `bot`, `infrastructure`, `shared`, `domain`, `config`.
- Оновлення існуючого README після суттєвого рефакторингу.
- Внутрішні підкаталоги (наприклад, `shared/prompts/uk`) з окремим змістом.

---

## 🧱 Обов’язковий каркас
1. **H1 із емодзі та назвою** (`# 🚚 Delivery Infrastructure`), далі 1–2 речення про роль модуля.
2. Розділи відокремлюємо `---` — це візуальний ритм усіх README в репозиторії.
3. **Структура каталогу** (`## 📂 Структура` або `## 📂 Вміст`) у вигляді дерева:
   ```bash
   package/
   ├── 📘 README.md
   ├── 📄 __init__.py
   └── ...
   ```
4. **Призначення/Обов’язки** (`## 🧭 Призначення`) — 3–5 bullet-пунктів, що пояснюють межі відповідальності, зв’язок із доменом та ключові контракти.
5. **Ключові файли/сервіси** (`## 🧱 Сервіси`, `## 🧩 Компоненти` тощо) — маркований список, де кожен пункт форматується як `- **\`file.py\`** — короткий опис, максимум 2 речення.`.
6. **Конфігурація / Контракти** за потреби:
   - YAML-блок із прикладом параметрів (`## ⚙️ Конфігурація`).
   - Python-блок із посиланням на доменний протокол (`## 🔗 Контракт домену`).
7. **Приклад використання** (`## 🚀 Приклад використання`) — мінімальний snippet (переважно Python) з реальними імпортами (`from app.infrastructure...`).
8. **Гарантії/Примітки** (`## ✅ Примітки`, `## ✅ Стиль`) — короткий список правил, обмежень або майбутніх кроків.

---

## ✍️ Правила оформлення
- **Мова** — українська; терміни домену допускаються англійською, але контекст пояснюємо.
- **Емодзі**: один на заголовок, узгоджений із сенсом секції (📂 для структури, 🚀 для прикладу тощо).
- **Абзаци**: не більше двох речень між розділювачами; складні переліки виносимо в списки.
- **Код-блоки**: завжди з info string (` ```python`, ` ```yaml`, ` ```bash` ).
- **Посилання**: внутрішні модулі оформлюємо як інлайнові посилання на файли (`[`IDeliveryService`](../../domain/delivery/interfaces.py)`).

---

## 🧩 Опційні секції
> Використовуйте за потреби, якщо вони додають змістовності.

- `## 🧪 Тестування` — якщо каталог містить тестові сценарії або вимоги до них.
- `## 📊 Метрики` — для каталогів, що експортують Prometheus/іншу телеметрію.
- `## 🔄 Життєвий цикл` — коли важливо описати ініціалізацію/шедулінг/фонові задачі.

---

## ✅ Чекліст перед комітом
- [ ] Заголовок і короткий опис присутні.
- [ ] Є дерево каталогу з актуальними файлами.
- [ ] Описано призначення та ключові компоненти.
- [ ] Додано приклад використання (якщо модуль надає API).
- [ ] Конфігурація/контракти згадані, якщо модуль їх використовує.
- [ ] Файл відповідає тону та емодзі-стилю інших README.

---

## 📎 Приклади для натхнення
- `src/app/shared/README.md` — великий пакет із кількома підрівнями.
- `src/app/infrastructure/delivery/README.md` — проста структура + приклад.
- `src/app/infrastructure/data_storage/README.md` — наочний блок конфігурації й контракту.

Завжди синхронізуй README з реальними файлами, щоб він залишався джерелом правди для всієї команди. 😊
