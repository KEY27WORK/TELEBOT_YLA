# 🇺🇦 Ukrainian Prompts

Каталог **`app/shared/prompts/uk`** містить текстові шаблони для LLM-підказок
українською мовою. Шаблони використовуються сервісом `PromptService`
(`app.shared.utils.prompt_service`) для генерації контенту.

## 📄 Файли

- `alt_text.txt` — інструкція для генерації ALT-текстів до зображень.
- `clothing_type.txt` — промт визначення типу одягу за описом/фото.
- `hashtags.txt` — добірка тематичних хештегів під товар.
- `music.txt` — підбір 10 музичних треків для Reels/Stories.
- `slogan.txt` — створення короткого дерзкого слогану з емодзі.
- `translation.txt` — переклад опису українською у фірмовому стилі.
- `weight.txt` — оцінка ваги товару на основі опису та зображення.

```bash
🇺🇦 uk/
├── 📘 README.md
├── 📄 alt_text.txt
├── 📄 clothing_type.txt
├── 📄 hashtags.txt
├── 📄 music.txt
├── 📄 slogan.txt
├── 📄 translation.txt
└── 📄 weight.txt
```

## 🧭 Потік використання

1. `PromptService.get_prompt(PromptType, **kwargs)` підставляє змінні (`{title}`, `{description}`, `{image_url}`).
2. Згенерований текст надсилається LLM (OpenAI, etc).
3. Отриманий результат повертається ботам/сервісам як готовий контент.

## 🛡️ Принципи

- Шаблони містять чіткі інструкції, формат відповіді та tone of voice.
- Всі файли зберігаються в UTF‑8, без коду Python.
- Змінні `{...}` підставляються сервісом перед відправкою в LLM.

## 🧪 Рекомендації

- Для тестування використовуйте `PromptService.load_text(...)` або `get_prompt(...)`.
- Переконайтесь, що всі потрібні плейсхолдери передані в `kwargs`, інакше форматування кине помилку.
